# Copie para .env.local (dev) ou configure no Render (build do Static Site).
# Em produção o proxy /api do Vite NÃO existe — use a URL completa da API.

# Modelo 1 (LSTM 1.2.0 + /ops)
VITE_API_BASE=http://127.0.0.1:8000

# App Flutter web
VITE_APP_BASE=http://127.0.0.1:5050
